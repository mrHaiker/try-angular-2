import {Component} from "@angular/core";@Component({  selector: "todos",  template: `    <h1>Todos</h1>        <div>      <input type="text" [(ngModel)]="newTodo" (keyup.enter)="addCar()"/>       <h3>{{ newTodo }}</h3>    </div>        <div *ngIf="todos">      <p>Todos is: {{ todos }}</p>    </div>    <div *ngIf="!todos">      <p>Not found</p>    </div>      <div [ngSwitch]="name">        <div *ngSwitchCase="'John'">Name is John</div>        <div *ngSwitchCase="'Bob'">Name is Bob</div>        <div *ngSwitchDefault>Name not found</div>    </div>    <br>        <div *ngIf="errorMsg" class="alert alert-danger">{{ errorMsg }}</div>    <div *ngIf="successMsg" class="alert alert-success">{{ successMsg }}</div>    <div *ngFor="let car of cars">        <a href="" (click)="removeCar(car)">{{ car }}</a>    </div>    <button (click)="resetCars()">Reset</button>  `,})export class TodosComponent {  todos: boolean;  name: string;  cars: string[];  newTodo: string;  private errorMsg: string;  private successMsg: string;  constructor() {    this.todos = true;    this.name = 'Bob';    this.cars = ['BMW', 'Tesla', "Ford"];  }  addCar() {    if (!this.newTodo ||  this.newTodo.length < 3) {      this.errorMsg = 'Name of car must be at least 3 characters';      this.successMsg = '';    } else {      this.cars.push(this.newTodo);      this.successMsg = 'Car added';      this.errorMsg = '';    }  }  removeCar(car: any) {    this.cars.splice(this.cars.indexOf(car), 1)  }  resetCars() {    this.cars = [];    this.successMsg = 'Cars array cleared';  }}